_0e1‍.x([["createQuery",()=>createQuery],["createBtn",()=>createBtn],["isNewUser",()=>isNewUser],["getUser",()=>getUser]]);const fs = require('fs');

class Query {
    constructor(entryMessageID, type, content){
        this.entryMessageID = entryMessageID;
        this.type = type;
        this.content = content;
    }
}

       const createQuery = (entryMessageID, type, content) => {
    let q = new Query(entryMessageID, type, content);
    return JSON.stringify(q);
}

       const createBtn = (text,data) => {
    return {
        text,
        callback_data:data
    }
}

       const isNewUser = (chat_id, allUsers) => {

    for (const item of allUsers) {
        if(item.chat_id === chat_id)
            return false;
    }
    return true;
}

       const getUser = (chat_id, allUsers)=>{
    console.log(allUsers)
    for (const item of allUsers) {
        console.log(item)
        if(item.chat_id === chat_id)
        return item;
    }
    console.log('asdfasdf')
    return null;
}

