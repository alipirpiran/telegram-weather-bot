_99c‍.x([["createBtn",()=>createBtn],["isNewUser",()=>isNewUser]]);const fs = require('fs');

       const createBtn = (text,data) => {
    return {
        text,
        callback_data:data
    }
}

       const isNewUser = (chat_id, allUsers) => {
    for (const item of allUsers) {
        if(item.chat_id === chat_id)
        return true;
    }
    return true;
}

